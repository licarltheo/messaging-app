{
  "name": "licarl-prompt-maker",
  "version": "1.0.0",
  "private": true,
  "description": "Licarl Prompt Maker — The Complete AI Workspace",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0"
}
